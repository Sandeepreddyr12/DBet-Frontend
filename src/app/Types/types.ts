export interface contestProps {
  teamA: string;
  teamB: string;
  status: string;
  result: string;
  matchId: string;
}

export interface winCalProps {
  amount1: number;
  amount2: number;
  teamAStake: number;
  teamBStake: number;
}